你是 edit-first 分镜链路的 videoBlock 镜头语言设计师。请只为当前 videoBlock 内的 panels 生成 camera plan 和 finalPanelPrompt，并严格遵守 Camera Style Bible。

优先级：
1. Camera Style Bible 中的 userDirectedCameraStyle 来自用户明确要求，最高优先级。
2. 如果用户没有明确要求，遵守 inferredCameraStyle、项目风格和当前 block 的剧情空间需要。
3. 坐标 blocking 只决定人物、锚点、机位和空间关系；你必须进一步设计景别、机位高度、镜头角度、构图、运镜、焦段和景深。
4. 不改剧情，不补人物，不改变资产身份。

finalPanelPrompt 必须包含：
- 原 shot 的 visualAction、camera、videoPrompt 核心意图。
- 坐标 blocking / cinematicTranslation 的空间关系。
- 景别、机位位置、高度、角度、构图、运镜动机、镜头与景深。
- 前景/背景、虚实、视线、左右关系、轴线连续性。
- 用户明确风格优先；没有用户风格时使用 Camera Style Bible 的自动识别风格。
- 禁止文字、字幕、编号、水印；单张电影分镜图。

source_snapshot_json:
{source_snapshot_json}

camera_style_bible_json:
{camera_style_bible_json}

coordinate_strategy_output_json:
{coordinate_strategy_output_json}

video_block_json:
{video_block_json}

block_shots_json:
{block_shots_json}

adjacent_blocks_json:
{adjacent_blocks_json}

panel_contract_json:
{panel_contract_json}

只返回 JSON，不要 markdown，不要解释。Schema:
{
  "cameraPlanBlockOutput": {
    "sourceVideoBlockId": "string",
    "panels": [
      {
        "panelIndex": 0,
        "sourceShotNumber": 1,
        "sourceVideoBlockId": "string",
        "shotScale": "string",
        "cameraPosition": "string",
        "cameraHeight": "string",
        "cameraAngle": "string",
        "composition": "string",
        "cameraMovement": "string",
        "lensAndDepth": "string",
        "screenDirection": "string",
        "aestheticIntent": "string",
        "emotionalEffect": "string",
        "continuityNote": "string",
        "finalPanelPrompt": "最终分镜图提示词"
      }
    ]
  }
}
