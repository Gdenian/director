请把以下剪辑镜头转换为单张分镜图画面。只生成这个镜头对应的一张画面，不扩写剧情，不改写原 shot 动作。

【镜头数据】
{shot_json}

【所属视频段】
{video_block_json}

【角色资产】
{character_assets_json}

【场景资产】
{location_assets_json}

【一致性实验输出：俯视坐标与镜头语言转换】
{strategy_output_json}

画面生成要求：
- 以 shot.videoPrompt 作为核心画面意图。
- 坐标图是俯视 blocking 规划，不是要画进画面里的网格或标签。
- 必须优先执行 strategy_output 中由坐标转换出的镜头语言描述：人物在锚点左前/右后、前景/背景、虚实、视线方向、左右关系。
- 不要把坐标值、网格线、坐标轴、数字或标签画进图像。
- 若本 shot 被标记为 no_fixed_space 或 skipped，只按原 shot 生成，不强行锁定人物位置。
- 这是一张电影分镜图，不要出现字幕、文字、编号、标签、水印或分屏。
