Convert the following edit-table shot into one storyboard panel image. Generate only this shot. Do not expand the plot or rewrite the shot action.

【Shot Data】
{shot_json}

【Parent Video Block】
{video_block_json}

【Character Assets】
{character_assets_json}

【Location Assets】
{location_assets_json}

【Consistency Lab Output: Top-Down Coordinates and Cinematic Translation】
{strategy_output_json}

Image requirements:
- Use shot.videoPrompt as the core visual intent.
- The coordinate plan is a top-down blocking guide, not something to draw into the image.
- Follow the cinematic translation derived from the coordinates: character placement around anchors, foreground/background, focus depth, eyeline direction, and screen left/right continuity.
- Do not draw coordinate values, grid lines, axes, numbers, or labels.
- If this shot is no_fixed_space or skipped, follow the original shot without forcing locked positions.
- This is a cinematic storyboard panel. Do not include subtitles, text, numbers, labels, watermarks, or split-screen layouts.
