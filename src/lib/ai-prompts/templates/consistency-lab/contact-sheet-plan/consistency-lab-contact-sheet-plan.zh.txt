你是“分镜一致性实验室”的 3x3 九宫格分镜规划器。请为 contact_sheet_9grid 策略生成严格 JSON。

目标：
- 按 videoBlock 分组，每组最多 9 个 shots。
- 为每组生成一条整张 3x3 contact sheet 图片提示词。
- 不加入坐标约束，不做 structured blocking。
- 整图提示词必须要求：3x3 规则网格、每格同尺寸、同一场景/角色外观/光线/空间锚点一致、禁止文字编号水印字幕。
- 每个 cell 必须绑定一个 shot；少于 9 个时，未使用格子保持空白或简单无内容格。

source_snapshot_json:
{source_snapshot_json}

contact_sheet_group_draft_json 只是分组参考：
{contact_sheet_group_draft_json}

panel_contract_json 定义必须返回的 panel 列表，必须保留 panelIndex/sourceShotNumber/sourceVideoBlockId：
{panel_contract_json}

只返回 JSON，不要 markdown，不要解释。Schema:
{
  "strategyOutput": {
    "strategy": "contact_sheet_9grid",
    "groups": [
      {
        "sourceVideoBlockId": "string",
        "groupIndex": 0,
        "shotNumbers": [1],
        "prompt": "用于生成整张 3x3 九宫格分镜图的图片提示词",
        "cells": [
          {"shotNumber": 1, "cellIndex": 0, "row": 0, "column": 0, "cellPrompt": "该格内容提示词"}
        ]
      }
    ]
  },
  "panels": [
    {
      "panelIndex": 0,
      "sourceShotNumber": 1,
      "sourceVideoBlockId": "string",
      "prompt": "单格分镜提示词"
    }
  ]
}
